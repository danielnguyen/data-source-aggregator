# Optional service-level overrides for local non-Docker runs.
# Docker Compose already sets these paths at the service level.
SOURCE_CONFIG_DIR=config/sources
AUDIT_LOG_PATH=var/audit/events.jsonl
CREDENTIALS_CONFIG_PATH=config/credentials.yaml
